<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Suraksha - Tourist Login</title>
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    /* ---------- Theme & layout ---------- */
    :root { --saffron-500: #ff9933; --royal-blue: #1E3A8A; }

    body {
      background: linear-gradient(135deg, #fff8e7 0%, #fff3e0 100%);
      min-height: 100vh;
      font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      position: relative;
      overflow-x: hidden;
      margin: 0;
    }

    .royal-texture { }

    .floating-elements { position: absolute; width:100%; height:100%; pointer-events:none; z-index:-1; }
    .floating-elements::before,
    .floating-elements::after {
      content: '';
      position: absolute;
      width: 320px; height: 320px; border-radius: 50%;
      background: radial-gradient(circle, rgba(255,153,51,0.12) 0%, transparent 70%);
      animation: float 6s ease-in-out infinite;
    }
    .floating-elements::before { top: 5%; left: 5%; }
    .floating-elements::after { bottom: 5%; right: 5%; animation-delay: 3s; }
    @keyframes float { 0%,100%{transform:translateY(0)} 50%{transform:translateY(-18px)} }

    .hero-pattern { background-image: radial-gradient(circle at 50% 50%, rgba(255,153,51,0.06) 0%, transparent 70%); }

    .gradient-saffron { background: linear-gradient(135deg, var(--saffron-500) 0%, #ffb74d 100%); }
    .shadow-royal { box-shadow: 0 20px 60px rgba(255,153,51,0.12); }

    .btn-royal {
      background: linear-gradient(135deg, var(--saffron-500) 0%, #ffb74d 70%, var(--saffron-500) 100%);
      transition: all 0.35s ease;
    }
    .btn-royal:hover { transform: translateY(-2px); box-shadow: 0 15px 35px rgba(255,153,51,0.35); }

    .card-royal {
      background: rgba(255,255,255,0.98);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255,153,51,0.12);
      border-radius: 1rem;
      padding: 2rem;
    }

    .text-royalblue { color: var(--royal-blue); }
    .text-saffron { color: var(--saffron-500); }

    /* Form / Inputs */
    .login-container { min-height: 100vh; display:flex; align-items:center; justify-content:center; padding:2rem 1rem; }
    .w-full.max-w-md { width:100%; max-width: 28rem; box-sizing:border-box; }

    .input-group { position: relative; margin-bottom: 1.25rem; }

    .form-input {
      width: 100%; box-sizing: border-box; padding: 0.85rem 1rem; padding-left: 2.75rem;
      border-radius: 0.75rem; border: 2px solid rgba(229,231,235,1); background: rgba(255,255,255,0.95);
      transition: all 0.25s ease;
    }
    .form-input:focus { outline: none; transform: translateY(-2px); box-shadow: 0 8px 25px rgba(255,153,51,0.12); border-color: rgba(255,153,51,0.25); }

    .input-icon { position: absolute; left: 12px; top: 50%; transform: translateY(-50%); z-index: 10; color: #9CA3AF; pointer-events: none; }
    .form-input:focus + .input-icon, .form-input:not(:placeholder-shown) + .input-icon { color: var(--saffron-500); }

    /* Password toggle (right) */
    .toggle-password-btn { position: absolute; right: 12px; top: 50%; transform: translateY(-50%); background: transparent; border: none; cursor: pointer; color: #9CA3AF; }
    .toggle-password-btn:hover { color: var(--saffron-500); }

    /* Strength indicator */
    .strength-indicator { height: 6px; background: #E5E7EB; border-radius: 999px; margin-top: 0.5rem; overflow: hidden; }
    .strength-bar { height: 100%; width: 0%; transition: all 0.3s ease; border-radius: 999px; }
    .strength-weak { width: 25%; background: #EF4444; }
    .strength-fair { width: 50%; background: #F59E0B; }
    .strength-good { width: 75%; background: #10B981; }
    .strength-strong { width: 100%; background: #059669; }

    /* Notification */
    .notification { position: fixed; top: 20px; right: 20px; padding: 1rem 1.25rem; border-radius: 12px; color: #fff; font-weight:500; transform: translateX(120%); transition: transform 0.3s ease; z-index: 1000; }
    .notification.show { transform: translateX(0); }
    .notification.success { background: linear-gradient(135deg,#10B981 0%, #059669 100%); }
    .notification.error { background: linear-gradient(135deg,#EF4444 0%, #DC2626 100%); }

    .loading-spinner { width:18px; height:18px; border:2px solid transparent; border-top:2px solid currentColor; border-radius:50%; animation: spin 1s linear infinite; }
    @keyframes spin { 0%{transform:rotate(0)} 100%{transform:rotate(360deg)} }

    /* Features grid & footer */
    .features-grid { display:grid; grid-template-columns: repeat(3,1fr); gap:1rem; margin-top:1.5rem; }
    .feature-item { text-align:center; padding:0.9rem; background: rgba(255,255,255,0.55); border-radius: 12px; border: 1px solid rgba(255,153,51,0.06); }

    @media (max-width:768px) {
      .features-grid { grid-template-columns: 1fr; }
      .logo-title { font-size: 2.25rem; }
      .w-full.max-w-md { max-width: 100% !important; }
      .floating-elements { display:none; }
    }
  </style>
</head>
<body class="royal-texture">
  <div class="floating-elements" aria-hidden="true"></div>

  <div class="login-container hero-pattern">
    <div class="w-full max-w-md">

      <!-- Logo -->
      <div class="logo-section text-center" style="margin-bottom:1.5rem;">
        <h1 class="logo-title" style="font-size:3rem; font-weight:800; background: linear-gradient(135deg,#FF9933 0%, #FFB74D 100%); -webkit-background-clip:text; -webkit-text-fill-color:transparent;">Surakshā</h1>
        <p class="logo-subtitle" style="color:#6B7280; font-weight:500;">Your Companion for Safe Indian Journeys</p>
      </div>

      <!-- Card -->
      <div class="card-royal shadow-royal">
        <div class="text-center mb-6">
          <div class="gradient-saffron w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-4 shadow-royal">
            <svg class="w-10 h-10 text-white" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true"><path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"></path></svg>
          </div>
          <h2 class="text-2xl font-bold text-royalblue mb-2">Welcome Back, Traveler</h2>
          <p class="text-gray-600 text-sm">Sign in to continue your safe journey across India</p>
        </div>

        <!-- Login Form -->
        <form id="loginForm" class="space-y-6" onsubmit="processLogin(event)">

          <!-- Email -->
          <div class="input-group">
            <label for="loginEmail" class="block text-sm font-semibold text-royalblue mb-2">Email Address</label>
            <div class="relative">
              <input id="loginEmail" name="loginEmail" type="email" required placeholder="Enter your email" class="form-input" aria-label="Email address" />
              <!-- icon (placed after input so "input:focus + .input-icon" works) -->
              <svg class="input-icon w-5 h-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"></path><path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"></path></svg>
            </div>
          </div>

          <!-- Password -->
          <div class="input-group">
            <label for="loginPassword" class="block text-sm font-semibold text-royalblue mb-2">Password</label>
            <div class="relative">
              <input id="loginPassword" type="password" required placeholder="Enter your password" class="form-input" oninput="checkPasswordStrength(this.value)" aria-label="Password" />

              <!-- left icon -->
              <svg class="input-icon w-5 h-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"></path></svg>

              <!-- toggle button (right) contains two svgs and we toggle their display) -->
              <button type="button" id="togglePasswordBtn" class="toggle-password-btn" aria-label="Toggle password visibility" onclick="togglePasswordVisibility()">
                <!-- eye-closed icon (visible by default) -->
                <svg id="eyeClosed" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.478 0-8.268-2.943-9.543-7a9.97 9.97 0 012.392-3.918M6.1 6.1A9.966 9.966 0 0112 5c4.478 0 8.268 2.943 9.543 7a10.015 10.015 0 01-4.4 5.192"/></svg>
                <!-- eye-open icon (hidden by default) -->
                <svg id="eyeOpen" class="w-5 h-5" style="display:none" viewBox="0 0 24 24" fill="none" stroke="currentColor" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
              </button>
            </div>

            <!-- strength indicator -->
            <div id="strengthIndicator" class="strength-indicator" style="display:none">
              <div id="strengthBar" class="strength-bar"></div>
            </div>
          </div>

          <!-- remember & forgot -->
          <div class="flex items-center justify-between">
            <label class="flex items-center cursor-pointer">
              <input type="checkbox" class="rounded border-gray-300 text-saffron focus:ring-saffron" />
              <span class="ml-2 text-sm text-gray-600">Remember me</span>
            </label>
            <a href="#" onclick="showForgotPassword(); return false;" class="text-sm text-saffron hover:text-orange-600 font-medium">Forgot password?</a>
          </div>

          <!-- submit -->
          <button id="loginBtn" type="submit" class="w-full btn-royal text-white py-3 rounded-xl font-semibold text-lg shadow-royal flex items-center justify-center" aria-live="polite">
            <span id="loginBtnText">Sign In to Surakshā</span>
            <div id="loginSpinner" class="loading-spinner ml-2" style="display:none" aria-hidden="true"></div>
          </button>
        </form>

        <!-- social -->
        <div class="relative my-6">
          <div class="absolute inset-0 flex items-center"><div class="w-full border-t border-gray-200"></div></div>
          <div class="relative flex justify-center text-sm"><span class="px-4 bg-white text-gray-500 font-medium">Or continue with</span></div>
        </div>

        <div class="grid grid-cols-2 gap-3">
          <button type="button" onclick="socialLogin('google')" class="flex justify-center items-center px-4 py-2 border border-gray-300 rounded-lg shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50">
            <!-- google svg -->
            <svg class="w-4 h-4 mr-2" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
              <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
              <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
              <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
            </svg>
            Google
          </button>

          <button type="button" onclick="socialLogin('facebook')" class="flex justify-center items-center px-4 py-2 border border-gray-300 rounded-lg shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50">
            <svg class="w-4 h-4 mr-2" viewBox="0 0 24 24" fill="#1877F2" aria-hidden="true"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
            Facebook
          </button>
        </div>

        <!-- divider -> register -->
        <div class="relative my-8">
          <div class="absolute inset-0 flex items-center"><div class="w-full border-t border-gray-200"></div></div>
          <div class="relative flex justify-center text-sm"><span class="px-4 bg-white text-gray-500 font-medium">New to our platform?</span></div>
        </div>

        <div class="text-center space-y-4">
          <p class="text-gray-600 text-sm">Join thousands of safe travelers exploring India</p>
          <div class="space-y-3">
            <button type="button" onclick="navigateTo('/signup')" class="register-btn w-full py-3 rounded-xl font-semibold text-lg" style="background:rgba(255,255,255,0.9); border:2px solid var(--saffron-500); color:var(--saffron-500); transition:all .25s;">Create Your Free Account</button>
            <div class="text-xs text-gray-500">🚀 Quick 3-step registration • 📱 Mobile verification • 🛡️ Emergency contacts</div>
          </div>
        </div>

        <!-- trust indicators -->
        <div class="mt-6 pt-6 border-t border-gray-100">
          <div class="flex items-center justify-center space-x-6 text-xs text-gray-500">
            <div class="flex items-center space-x-1"><svg class="w-4 h-4 text-saffron" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"></path></svg><span class="font-medium">256-bit SSL</span></div>
            <div class="flex items-center space-x-1"><svg class="w-4 h-4 text-saffron" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg><span class="font-medium">Privacy Protected</span></div>
            <div class="flex items-center space-x-1"><svg class="w-4 h-4 text-saffron" fill="currentColor" viewBox="0 0 20 20"><path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg><span class="font-medium">Verified Safe</span></div>
          </div>
        </div>

      </div> <!-- card -->

      <!-- features grid -->
      <div class="features-grid">
        <div class="feature-item"><div class="feature-icon"><svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"></path></svg></div><p class="text-xs font-medium text-royalblue">Real-time Safety</p></div>
        <div class="feature-item"><div class="feature-icon"><svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20"><path d="M2 3a1 1 0 011-1h2.153a1 1 0 01.986.836l.74 4.435a1 1 0 01-.54 1.06l-1.548.773a11.037 11.037 0 006.105 6.105l.774-1.548a1 1 0 011.059-.54l4.435.74a1 1 0 01.836.986V17a1 1 0 01-1 1h-2C7.82 18 2 12.18 2 5V3z"></path></svg></div><p class="text-xs font-medium text-royalblue">Emergency SOS</p></div>
        <div class="feature-item"><div class="feature-icon"><svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M18 13V5a2 2 0 00-2-2H4a2 2 0 00-2 2v8a2 2 0 002 2h3l3 3 3-3h3a2 2 0 002-2zM5 7a1 1 0 011-1h8a1 1 0 110 2H6a1 1 0 01-1-1zm1 3a1 1 0 100 2h3a1 1 0 100-2H6z" clip-rule="evenodd"></path></svg></div><p class="text-xs font-medium text-royalblue">24/7 Support</p></div>
      </div>

      <!-- footer -->
      <div class="text-center mt-8">
        <p class="text-xs text-gray-500">© 2025 Surakshā. Keeping travelers safe across India.</p>
        <div class="flex justify-center space-x-4 mt-2"><a href="#" class="text-xs text-saffron hover:text-orange-600">Privacy Policy</a><a href="#" class="text-xs text-saffron hover:text-orange-600">Terms of Service</a><a href="#" class="text-xs text-saffron hover:text-orange-600">Help Center</a></div>
      </div>

    </div>
  </div>

  <!-- notification container -->
  <div id="notificationContainer" aria-live="polite"></div>

  <!-- --- Scripts: moved to bottom (cleaned & fixed) --- -->
  <script>
    function navigateTo(route) {
            window.location.href = route;
        }

    function checkPasswordStrength(password) {
      const indicator = document.getElementById('strengthIndicator');
      const bar = document.getElementById('strengthBar');
      if (!indicator || !bar) return;

      if (!password || password.length === 0) {
        indicator.style.display = 'none';
        bar.className = 'strength-bar';
        return;
      }

      indicator.style.display = 'block';
      let strength = 0;
      if (password.length >= 8) strength++;
      if (/[A-Z]/.test(password)) strength++;
      if (/[0-9]/.test(password)) strength++;
      if (/[^A-Za-z0-9]/.test(password)) strength++;

      bar.className = 'strength-bar';
      switch (strength) {
        case 0: bar.style.width = '0%'; break;
        case 1: bar.classList.add('strength-weak'); break;
        case 2: bar.classList.add('strength-fair'); break;
        case 3: bar.classList.add('strength-good'); break;
        case 4: bar.classList.add('strength-strong'); break;
      }
    }

    function isValidEmail(email) {
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      return emailRegex.test(email);
    }

    // Notifications
    function showNotification(message, type = 'success') {
      const container = document.getElementById('notificationContainer');
      if (!container) return;

      const notification = document.createElement('div');
      notification.className = `notification ${type}`;
      notification.innerHTML = `
        <div style="display:flex;align-items:center;gap:0.5rem">
          <div style="flex-shrink:0">${type === 'success' ? '<svg class="w-5 h-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>' : '<svg class="w-5 h-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path></svg>'}</div>
          <div style="font-size:0.95rem">${message}</div>
          <div style="margin-left:auto"><button class="dismiss-btn" style="background:transparent;border:none;color:inherit;cursor:pointer;padding:4px" aria-label="Dismiss">&times;</button></div>
        </div>
      `;

      container.appendChild(notification);
      // small delay to animate in
      setTimeout(() => notification.classList.add('show'), 50);

      // dismiss handler
      const btn = notification.querySelector('.dismiss-btn');
      btn.addEventListener('click', () => hideNotification(btn));

      // auto hide
      setTimeout(() => {
        hideNotification(btn);
      }, 5000);
    }

    function hideNotification(buttonOrElem) {
      if (!buttonOrElem) return;
      const notification = buttonOrElem.closest ? buttonOrElem.closest('.notification') : buttonOrElem;
      if (!notification) return;
      notification.classList.remove('show');
      setTimeout(() => { try { notification.remove(); } catch (e) {} }, 300);
    }

    // Form submit simulation
    function processLogin(e) {
      e && e.preventDefault();
      const email = document.getElementById('loginEmail').value.trim();
      const password = document.getElementById('loginPassword').value;

      if (!email || !password) { showNotification('Please fill in all fields', 'error'); return; }
      if (!isValidEmail(email)) { showNotification('Please enter a valid email address', 'error'); return; }

      const loginBtn = document.getElementById('loginBtn');
      const loginBtnText = document.getElementById('loginBtnText');
      const loginSpinner = document.getElementById('loginSpinner');

      loginBtnText.textContent = 'Signing you in...';
      loginSpinner.style.display = 'inline-block';
      loginBtn.disabled = true; loginBtn.style.opacity = '0.7';

      // fake API call
      setTimeout(() => {
        showNotification('Login successful! Welcome back to Surakshā', 'success');
        // reset
        document.getElementById('loginEmail').value = '';
        document.getElementById('loginPassword').value = '';
        document.getElementById('strengthIndicator').style.display = 'none';

        loginBtnText.textContent = 'Sign In to Surakshā';
        loginSpinner.style.display = 'none';
        loginBtn.disabled = false; loginBtn.style.opacity = '1';

        // redirect placeholder (uncomment in production)
        // window.location.href = '/dashboard';
      }, 1400);
    }

    function showSignup() { showNotification('Redirecting to registration...', 'success'); setTimeout(() => { /* window.location.href='/signup' */ }, 700); }

    function socialLogin(provider) {
      showNotification(`Connecting with ${provider.charAt(0).toUpperCase() + provider.slice(1)}...`, 'success');
      setTimeout(() => showNotification(`${provider.charAt(0).toUpperCase() + provider.slice(1)} login is currently in development`, 'error'), 1300);
    }

    function showForgotPassword() { showNotification('Password reset link would be sent to your email', 'success'); }

    function togglePasswordVisibility() {
      const pwd = document.getElementById('loginPassword');
      const eyeOpen = document.getElementById('eyeOpen');
      const eyeClosed = document.getElementById('eyeClosed');
      if (!pwd) return;
      if (pwd.type === 'password') {
        pwd.type = 'text';
        eyeOpen.style.display = 'inline-block';
        eyeClosed.style.display = 'none';
      } else {
        pwd.type = 'password';
        eyeOpen.style.display = 'none';
        eyeClosed.style.display = 'inline-block';
      }
    }

    // small UX helpers
    document.addEventListener('DOMContentLoaded', function() {
      const email = document.getElementById('loginEmail'); if (email) email.focus();
      setTimeout(() => showNotification('Welcome to Surakshā! Please sign in to continue.', 'success'), 900);

      // ctrl+enter submits
      document.addEventListener('keydown', function(e) { if (e.key === 'Enter' && e.ctrlKey) { document.getElementById('loginForm').dispatchEvent(new Event('submit')) } });

      // add focus scale effect
      document.querySelectorAll('.form-input').forEach(input => {
        input.addEventListener('focus', function(){ this.parentElement.classList.add('scale-105') });
        input.addEventListener('blur', function(){ this.parentElement.classList.remove('scale-105') });
      });
    });
  </script>
</body>
</html>
